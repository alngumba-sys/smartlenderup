
  # Smartlenderup

  This is a code bundle for Smartlenderup. The original project is available at https://www.figma.com/design/zV3aC7NuNg2Sg37f5e2Llx/Smartlenderup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  