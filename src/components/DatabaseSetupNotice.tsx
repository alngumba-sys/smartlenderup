import { useState, useEffect } from 'react';
import { AlertCircle, X, ExternalLink, Database } from 'lucide-react';
import { supabase } from '../lib/supabase';

export function DatabaseSetupNotice() {
  // DISABLED: Database is now properly configured
  // This component will not show anymore
  return null;
}