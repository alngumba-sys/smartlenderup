import { X } from 'lucide-react';
import { useTheme } from '../../contexts/ThemeContext';
import { formatCurrency } from '../../utils/currencyUtils';

interface DebugLoanDataModalProps {
  loan: any;
  loanPayments: any[];
  totalPaid: number;
  principalPaid: number;
  interestPaid: number;
  totalRepayable: number;
  outstandingBalance: number;
  payoffQuote: number;
  onClose: () => void;
}

export function DebugLoanDataModal({
  loan,
  loanPayments,
  totalPaid,
  principalPaid,
  interestPaid,
  totalRepayable,
  outstandingBalance,
  payoffQuote,
  onClose
}: DebugLoanDataModalProps) {
  const { isDark } = useTheme();

  return (
    <div className="fixed inset-0 bg-black bg-opacity-70 flex items-center justify-center z-[60] p-4">
      <div className={`w-full max-w-4xl max-h-[90vh] rounded-lg shadow-xl overflow-hidden flex flex-col ${\n        isDark ? 'bg-gray-800' : 'bg-white'\n      }`}>\n        <div className={`px-6 py-4 border-b flex items-center justify-between ${\n          isDark ? 'bg-amber-900 border-amber-700' : 'bg-amber-500 border-amber-400'\n        }`}>\n          <div className="flex items-center gap-3">\n            <svg className="size-6 text-white" fill="none" viewBox="0 0 24 24" stroke="currentColor">\n              <path strokeLinecap="round" strokeLinejoin="round" strokeWidth={2} d="M12 8v4m0 4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z" />\n            </svg>\n            <h2 className="text-xl text-white font-semibold">Debug: Raw Loan Data</h2>\n          </div>\n          <button\n            onClick={onClose}\n            className="p-2 rounded-lg hover:bg-white/10 text-white transition-colors"\n          >\n            <X className="size-5" />\n          </button>\n        </div>\n        \n        <div className="flex-1 overflow-y-auto p-6">\n          <div className="space-y-6">\n            {/* Payment Calculation Logic */}\n            <div className={`p-4 rounded-lg border ${isDark ? 'bg-gray-700 border-gray-600' : 'bg-gray-50 border-gray-200'}`}>\n              <h3 className={`text-lg font-semibold mb-3 ${isDark ? 'text-white' : 'text-gray-900'}`}>\n                💰 Payment Calculation Logic\n              </h3>\n              <div className="space-y-2 font-mono text-sm">\n                <div className={`p-2 rounded ${isDark ? 'bg-gray-800' : 'bg-white'}`}>\n                  <span className={`${isDark ? 'text-gray-400' : 'text-gray-600'}`}>Total Paid = </span>\n                  <span className={`${isDark ? 'text-emerald-400' : 'text-emerald-600'} font-bold`}>\n                    loan.amount_paid || loan.amountPaid || loan.totalPaid || sum(payments)
                  </span>\n                </div>\n                <div className={`p-2 rounded ${isDark ? 'bg-gray-800' : 'bg-white'}`}>\n                  <span className={`${isDark ? 'text-gray-400' : 'text-gray-600'}`}>loan.amount_paid = </span>\n                  <span className={`${isDark ? 'text-yellow-400' : 'text-yellow-600'} font-bold`}>\n                    {loan.amount_paid !== undefined ? formatCurrency(loan.amount_paid) : 'undefined'}
                  </span>\n                </div>\n                <div className={`p-2 rounded ${isDark ? 'bg-gray-800' : 'bg-white'}`}>\n                  <span className={`${isDark ? 'text-gray-400' : 'text-gray-600'}`}>loan.amountPaid = </span>\n                  <span className={`${isDark ? 'text-yellow-400' : 'text-yellow-600'} font-bold`}>\n                    {loan.amountPaid !== undefined ? formatCurrency(loan.amountPaid) : 'undefined'}
                  </span>\n                </div>\n                <div className={`p-2 rounded ${isDark ? 'bg-gray-800' : 'bg-white'}`}>\n                  <span className={`${isDark ? 'text-gray-400' : 'text-gray-600'}`}>loan.totalPaid = </span>\n                  <span className={`${isDark ? 'text-yellow-400' : 'text-yellow-600'} font-bold`}>\n                    {loan.totalPaid !== undefined ? formatCurrency(loan.totalPaid) : 'undefined'}
                  </span>\n                </div>\n                <div className={`p-2 rounded ${isDark ? 'bg-gray-800' : 'bg-white'}`}>\n                  <span className={`${isDark ? 'text-gray-400' : 'text-gray-600'}`}>Sum of payment records = </span>\n                  <span className={`${isDark ? 'text-yellow-400' : 'text-yellow-600'} font-bold`}>\n                    {formatCurrency(loanPayments.reduce((sum: number, p: any) => sum + (p.amount || 0), 0))}\n                  </span>\n                </div>\n                <div className={`p-2 rounded ${isDark ? 'bg-emerald-900/30' : 'bg-emerald-50'} border-2 ${isDark ? 'border-emerald-700' : 'border-emerald-300'}`}>\n                  <span className={`${isDark ? 'text-emerald-400' : 'text-emerald-700'}`}>✅ FINAL VALUE USED = </span>\n                  <span className={`${isDark ? 'text-emerald-300' : 'text-emerald-900'} font-bold text-lg`}>\n                    {formatCurrency(totalPaid)}\n                  </span>\n                </div>\n              </div>\n            </div>\n\n            {/* All Loan Fields */}\n            <div className={`p-4 rounded-lg border ${isDark ? 'bg-gray-700 border-gray-600' : 'bg-gray-50 border-gray-200'}`}>\n              <h3 className={`text-lg font-semibold mb-3 ${isDark ? 'text-white' : 'text-gray-900'}`}>\n                📋 All Loan Table Fields (Supabase)\n              </h3>\n              <div className="max-h-96 overflow-y-auto">\n                <pre className={`text-xs font-mono p-3 rounded whitespace-pre-wrap ${isDark ? 'bg-gray-800 text-gray-300' : 'bg-white text-gray-800'}`}>\n                  {JSON.stringify(loan, null, 2)}\n                </pre>\n              </div>\n            </div>\n\n            {/* Payment Records */}\n            <div className={`p-4 rounded-lg border ${isDark ? 'bg-gray-700 border-gray-600' : 'bg-gray-50 border-gray-200'}`}>\n              <h3 className={`text-lg font-semibold mb-3 ${isDark ? 'text-white' : 'text-gray-900'}`}>\n                💳 Payment Records for this Loan\n              </h3>\n              <div className={`p-2 rounded mb-3 ${isDark ? 'bg-blue-900/30' : 'bg-blue-50'}`}>\n                <span className={`${isDark ? 'text-blue-400' : 'text-blue-700'}`}>Found {loanPayments.length} payment record(s) in payments table</span>\n              </div>\n              {loanPayments.length === 0 ? (\n                <div className={`p-3 rounded text-center ${isDark ? 'bg-gray-800 text-gray-400' : 'bg-white text-gray-600'}`}>\n                  No payment records found in the payments table for this loan ID.\n                  <div className="mt-2 text-xs">\n                    This means payments are tracked ONLY in the loan table's amount_paid column.\n                  </div>\n                </div>\n              ) : (\n                <div className="max-h-96 overflow-y-auto space-y-2">\n                  {loanPayments.map((payment: any, index: number) => (\n                    <div key={payment.id} className={`p-3 rounded ${isDark ? 'bg-gray-800' : 'bg-white'}`}>\n                      <div className="flex justify-between mb-1">\n                        <span className={`font-semibold ${isDark ? 'text-white' : 'text-gray-900'}`}>\n                          Payment #{index + 1}\n                        </span>\n                        <span className={`font-bold ${isDark ? 'text-emerald-400' : 'text-emerald-600'}`}>\n                          {formatCurrency(payment.amount || 0)}\n                        </span>\n                      </div>\n                      <pre className={`text-xs font-mono whitespace-pre-wrap ${isDark ? 'text-gray-400' : 'text-gray-600'}`}>\n                        {JSON.stringify(payment, null, 2)}\n                      </pre>\n                    </div>\n                  ))}\n                </div>\n              )}\n            </div>\n\n            {/* Calculation Summary */}\n            <div className={`p-4 rounded-lg border ${isDark ? 'bg-gray-700 border-gray-600' : 'bg-gray-50 border-gray-200'}`}>\n              <h3 className={`text-lg font-semibold mb-3 ${isDark ? 'text-white' : 'text-gray-900'}`}>\n                🧮 Calculation Summary\n              </h3>\n              <div className="space-y-2">\n                <div className="flex justify-between">\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Total Repayable:</span>\n                  <span className={isDark ? 'text-white' : 'text-gray-900'}>{formatCurrency(totalRepayable)}</span>\n                </div>\n                <div className="flex justify-between">\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Total Paid (DISPLAYED):</span>\n                  <span className={`font-bold ${isDark ? 'text-emerald-400' : 'text-emerald-600'}`}>{formatCurrency(totalPaid)}</span>\n                </div>\n                <div className="flex justify-between">\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Principal Paid (DISPLAYED):</span>\n                  <span className={`font-bold ${isDark ? 'text-emerald-400' : 'text-emerald-600'}`}>{formatCurrency(principalPaid)}</span>\n                </div>\n                <div className="flex justify-between">\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Interest Paid (DISPLAYED):</span>\n                  <span className={`font-bold ${isDark ? 'text-emerald-400' : 'text-emerald-600'}`}>{formatCurrency(interestPaid)}</span>\n                </div>\n                <div className="flex justify-between">\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Sum from payment records:</span>\n                  <span className={isDark ? 'text-yellow-400' : 'text-yellow-600'}>\n                    {formatCurrency(loanPayments.reduce((sum: number, p: any) => sum + (p.amount || 0), 0))}\n                  </span>\n                </div>\n                <div className={`flex justify-between pt-2 border-t ${isDark ? 'border-gray-600' : 'border-gray-300'}`}>\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Outstanding Balance:</span>\n                  <span className={`font-bold ${isDark ? 'text-white' : 'text-gray-900'}`}>{formatCurrency(outstandingBalance)}</span>\n                </div>\n                <div className="flex justify-between">\n                  <span className={isDark ? 'text-gray-400' : 'text-gray-600'}>Payoff Quote:</span>\n                  <span className={`font-bold ${isDark ? 'text-amber-400' : 'text-amber-600'}`}>{formatCurrency(payoffQuote)}</span>\n                </div>\n              </div>\n            </div>\n\n            {/* Instructions */}\n            <div className={`p-4 rounded-lg border-2 ${isDark ? 'bg-blue-900/20 border-blue-700' : 'bg-blue-50 border-blue-300'}`}>\n              <h3 className={`text-lg font-semibold mb-2 ${isDark ? 'text-blue-400' : 'text-blue-900'}`}>\n                📌 How to Fix\n              </h3>\n              <div className={`text-sm space-y-1 ${isDark ? 'text-blue-300' : 'text-blue-800'}`}>\n                <p>1. Check which value is correct: <strong>28,750</strong> (expected) or <strong>{formatCurrency(totalPaid)}</strong> (displayed)</p>\n                <p>2. If the loan table's <code>amount_paid</code> column is wrong, update it in Supabase</p>\n                <p>3. If payment records are missing, check the repayments table in Supabase</p>\n                <p>4. Take a screenshot of this modal and share it with your developer</p>\n              </div>\n            </div>\n          </div>\n        </div>\n\n        <div className={`px-6 py-4 border-t ${isDark ? 'bg-gray-900 border-gray-700' : 'bg-gray-50 border-gray-200'}`}>\n          <button\n            onClick={onClose}\n            className="w-full px-4 py-2 bg-amber-600 hover:bg-amber-700 text-white rounded-lg transition-colors font-medium"\n          >\n            Close Debug View\n          </button>\n        </div>\n      </div>\n    </div>\n  );
}
